# REST API base. Must include /api/v1 suffix.
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Realtime WebSocket endpoint. When unset, derived from NEXT_PUBLIC_API_URL by
# swapping http(s)->ws(s) and appending /ws. Override for split domains.
NEXT_PUBLIC_WS_URL=ws://localhost:8080/api/v1/ws
